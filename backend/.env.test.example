# Test Environment Variables
# Copy this file to .env.test for running tests
# DO NOT commit .env.test with real values

# Server
NODE_ENV=test
PORT=3001
FRONTEND_URL=http://localhost:5173

# Database (use separate test database)
DATABASE_URL=postgresql://picai_user:password@localhost:5432/picai_test

# JWT
JWT_SECRET=test-jwt-secret-minimum-32-characters-long-for-security-testing

# Token Expirations (Note: These are defined in authService.ts and may not be used from env)
# Access token: 15 minutes (hardcoded in authService.ts)
# Refresh token: 7 days (hardcoded in authService.ts)
ACCESS_TOKEN_EXPIRATION=15m
REFRESH_TOKEN_EXPIRATION=7d

# Azure Computer Vision (use fake values for tests that don't call API)
AZURE_VISION_KEY=fake-test-key-for-testing-purposes-only
AZURE_VISION_ENDPOINT=https://fake-test-endpoint.cognitiveservices.azure.com/

# Storage (use temporary directories for tests)
UPLOAD_DIR=/tmp/picai-test/originals
THUMBNAIL_DIR=/tmp/picai-test/thumbnails
MAX_FILE_SIZE=26214400
